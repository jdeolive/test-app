{
  "name": "test-app",
  "version": "1.0.0",
  "devDependencies": {
    "http-server": "^0.12.1",
    "node-sass": "^4.13.0"
  },
  "scripts": {
    "build": "node-sass style.scss -o .",
    "start": "npm run build && node-sass -w style.scss -o . & http-server -c-1 ."
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "^5.12.0",
    "vue": "^2.6.11",
    "vuex": "^3.1.2"
  }
}
